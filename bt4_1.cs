// Mục tiêu: Xây dựng Login Form có Validation ErrorProvider, ẩn mật khẩu, gán AcceptButton/CancelButton.
// Yêu cầu (Requirements):
// Giao diện gồm: txtUsername, txtPassword, chkShowPassword, btnLogin, btnExit, errorProvider.
// Phím Enter kích hoạt btnLogin, phím Esc kích hoạt btnExit.
// chkShowPassword: 
// Tick chọn → Hiện mật khẩu (UseSystemPasswordChar = false)
// Bỏ chọn → Ẩn mật khẩu (*).
// Validation: Không để trống Tên đăng nhập & Mật khẩu bằng ErrorProvider.

